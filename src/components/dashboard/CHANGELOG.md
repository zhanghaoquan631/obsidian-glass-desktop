# 变更记录

## 2026-08-20（微信语音音量提示防误触发加固）

- 根据实际截图确认音量胶囊会在微信语音期间反复刷新；恢复自定义音量胶囊，并关闭 Windows 通信活动的自动衰减，让普通音量操作仍能正常显示。
- 将微信语音转文字期间的音量玻璃胶囊改为前台直接静默，并保留 6 秒收尾缓冲，覆盖语音输入过程中焦点短暂切换的情况；前台检测采用 0.75 秒缓存，音量变化时立即复核，不增加高频后台负担。
- 仅抑制自定义桌面音量提示；真实系统音量、微信语音转文字、音乐组件和亮度提示均不改变。
- 变更前文件已备份至 `backups/wechat-voice-volume-guard-20260820-215459`，登录启动继续沿用既有 `start.ps1` 链路。
- 已将重复的 MyDockFinder、桌面仪表盘和工作区启动快捷方式移至 `StartupBackups/centralized-startup-20260820-215802`，由已验证的统一登录任务单独启动，避免重复拉起同一提示层。

## 2026-08-20（微信语音通信音量提示过滤）

- 修复微信语音输入触发 Windows 通信音量短暂衰减时，音量玻璃胶囊误弹出的问题。
- 仅抑制“大幅下降后在短时间内恢复原值”的通信衰减模式；真实稳定的音量调整和静音操作仍会显示。
- 未修改微信、系统音量、Windows 核心文件或通信功能；修改前脚本已备份至 `backups/control-osd-wechat-ducking-20260820-082333`。
- 核对并实测统一登录任务、启动快捷方式和 `start.ps1` 链路；修正 `status.ps1` 对静默启动入口的误报。

## 2026-08-20（实时音量与亮度玻璃胶囊）

- 新增独立 `SystemControlOverlay.ps1`，使用 Windows Core Audio 与 WMI 实时读取音量、静音状态和显示器亮度。
- 按参考图实现半透明圆角胶囊、百分比、进度填充、方向指示和旋钮；上升采用暖黄色，下采用冷蓝色，实际控件名称保持准确。
- 接入 `start.ps1`、`stop.ps1` 和独立恢复脚本；不修改系统核心文件、危险注册表、Windows 原生 OSD、壁纸或已有桌面功能。
- 保存了替换前脚本、数据和两张参考图到 `backups/control-osd-pre-replacement-20260820-070626`。

## 2026-08-20（顶部参考图比例修正）

- 胶囊改为主屏正上方居中，并扩大为更接近参考图的横向比例。
- 上升状态复刻参考图一的 Brightness 暖黄色视觉；下降状态复刻参考图二的 Volume 冷蓝色视觉。
- 增强玻璃边缘、按钮高亮、百分比轨道和旋钮比例；真实控件名称保留在底部状态行。
- 增加 `-RenderPreview`，生成的上下状态 PNG 会覆盖项目日志目录中的旧预览，不覆盖原始参考备份。

## 2026-08-20（电影音乐链接与视频过滤）

- 影音历史改为只显示并保存电影和音乐；普通视频不再写入历史，实时双语字幕与播放器卡片继续保留。
- 历史条目新增来源链接：单击电影或音乐行即可复制到剪贴板，优先使用原网页或 SoundCloud 来源；无法还原原网页时明确复制对应服务或关键词的搜索链接。
- 旧版“电影/视频”数据会自动迁移：可确认的电影保留为电影，其他视频条目仍保留在数据文件中但不显示、不参与新的历史记录。

## 2026-08-20（影音识别准确性）

- 收紧系统音频兜底：只有已知播放器或标题明确的浏览器影音页面才会生成电影/视频记录，微信、普通工作窗口和任务切换不会再被误写入影音历史。
- 现有历史文件保持原样，不删除任何已保存的用户记录。

## 2026-08-20（开机分级启动与影音记录）

- 将字幕服务拆为“界面稳定后启动进程”和“稍后发送模型预热”两个阶段，实际开始播放或识别时仍可立即按需启动。
- 将媒体可视化改为延迟启动；空闲时约 420ms 低频刷新，检测到播放、声音或字幕监听后切换为 100ms 高频刷新。
- 新增可移动的“影音记录”组件，自动记录电影、视频和音乐的标题、来源、播放状态、当前位置、总时长以及处理器、显卡、内存和音频设备快照。
- 历史数据保存到 `data/media-history.json`，采用低频写盘和最多 80 条记录限制，不修改原媒体控制逻辑。

## 2026-08-20（影音记录滑动与桌面空闲降载）

- 影音记录列表改为真实可滚动面板，支持鼠标滚轮、触控板和触摸上下滑动；卡片标题栏仍可拖动整个组件。
- 媒体切换或停止时，上一条记录会标记为已结束；重新播放同一媒体会累计播放次数，避免历史条目长期停留在“播放中”。
- 组件被其他应用自动隐藏时保留音频与字幕检测，但跳过频谱条重绘，减少桌面切换和看电影时的 UI 负载。
- 电源历史扫描改为首屏完成后延迟执行；字幕工作进程使用低于普通的优先级，降低开机瞬时资源竞争。

## 2026-08-20（音乐可视化与影音记录合并）

- 将原音乐可视化和影音记录合并为同一个右侧 `MediaCard`，保留原绿色音乐可视化主体、播放按键、进度条、字幕和歌词。
- 记录列表放入主卡右侧，仍可上下滑动查看历史；旧的独立记录窗口不再创建，避免桌面出现两个媒体面板。
- 主卡扩展为 440×142 并贴齐工作区右边缘，未改变照片卡、模式卡、左侧栏、Dock 或壁纸的位置。

## 2026-08-20（模式切换可靠性与轻量刷新优化）

- 消除模式切换时重复执行恢复逻辑的问题，减少重复日志和无效窗口操作。
- 音乐模式改为复用已有媒体与可视化定时器，点击按钮时不再同步重复扫描媒体，切换响应更轻。
- 退出电影模式时清理空闲状态下残留的视频字幕配置；正在播放的视频仍交给实时媒体检测接管。
- AI 编程面板改为明确显示“本地配置”和“工具链提示”，避免误认为会修改 VS Code 或系统终端。

## 2026-08-20（恢复旧版动物陪伴音乐可视化）

- 按参考图恢复 300×142 的绿色音乐可视化卡片、音乐符号、彩色频谱、进度条和三键播放控制；保留实时媒体、电影对白和原文/简体中文字幕绑定。
- 恢复银渐层猫陪伴动画，待机时自主活动，鼠标悬停时保留轻微互动；不修改壁纸、Dock、左侧栏或其他桌面组件。
- 媒体卡改为仅桌面显示：电影、视频或其他应用进入前台后自动隐藏，字幕后台处理仍可继续，回到桌面后恢复显示。

## 2026-08-20（电影播放器外观恢复）

- 将右侧粉色媒体卡恢复为深色薄荷绿电影播放器视觉，保留原有位置、播放控制、媒体进度、实时双语字幕和语言选择。
- 恢复银渐层猫陪看元素及其待机、悬停互动；改为薄荷绿控制按钮和深色玻璃材质，不修改壁纸、Dock、左侧栏或其他桌面组件。

## 2026-08-16（右侧布局与 AI 捕捉滚动）

- 将三个模式卡拆成独立的电影、AI 编程、音乐面板，并改为右侧横向短布局，增加卡片间留白，避免纵向拉成长条。
- 应用使用时间卡改为浅色视觉并向右收窄；照片投放卡和音乐可视化卡保持原尺寸与坐标。
- 为照片卡和音乐卡增加固定锚点保护，避免桌面表面刷新或媒体状态切换时透明窗口漂移。
- 为 AI 捕捉与行动中心增加整卡纵向滚动，保留听写编辑、复制、清空和四条待办控件。

## 2026-08-16（模式中枢一键调优）

- 将电影、AI 编程、音乐三个按钮从单纯的本地状态切换升级为可观察的一键调优动作。
- 电影模式接入现有实时双语字幕准备和全屏桌面组件避让逻辑，不自动播放、不改系统音量。
- AI 编程模式接入现有 AI 捕捉与行动中心，并应用本地补全、工具链和终端推荐配置。
- 音乐模式接入现有媒体刷新、18 段可视化和同步歌词管线；滑杆会实时影响组件内可视化强度，不修改系统音频驱动。
- 增加模式动作状态提示、启动时恢复已保存的模式动作，以及点击关闭/重置时的本地可逆恢复。

## 2026-08-16（模式中枢与深空可见性）

- 新增可移动的“模式中枢”组件，提供电影、AI 编程、音乐三种本地调优面板、滑杆、开关和配置重置。
- 使用用户参考图作为低透明背景，保留原投放图片组件位置；移除天气“我的位置”和电量组件显示，将今日锁屏组件放入原电量位置。
- 应用使用时间组件改为黑曜石色，时钟组件改为金色，并为两者匹配本地动物素材。
- 修复 Lively 隐藏 WebView 导致十二星座和流星被误判为暂停的问题；现场验证可见星座连线、中文标签和流星。
- 新增 `AnimalTrailSystem/CHANGES-space-visibility-20260816.md` 记录修复原因、回退边界和验证证据。

## 2026-08-16

- 关闭固定 SoundCloud 曲目“依然与你同在 · Still With You”的开机自动播放。
- 统一开机任务不再启动隐藏 SoundCloud 播放器；若旧实例残留，启动任务会将其停止。
- 播放器页面和本地播放器均改为默认暂停的手动模式，音乐可视化组件与外部媒体字幕功能保留。
- 音乐组件在没有播放内容时显示“音乐已关闭”，避免继续显示旧曲目或请求旧歌词。

## 2026-08-15

- 修复音乐歌词时间轴：保留 LRC 间奏清空节点，上一句不会再跨越纯音乐段持续显示。
- 歌词会按播放器与歌词文件的实际时长小范围校准，并正确处理循环播放和向后跳转。
- 歌词翻译改为低并发重试，减少部分韩文歌词未生成简体中文的问题。
- 修复桌面组件开机自启在电池供电时被 Windows 跳过或停止的问题；统一登录任务现在允许电池启动、切换电池后继续运行，并已完成即时启动验证。
- 新增视频与电影实时双语字幕模式：Chrome、Edge、常见播放器及未提供 Windows 媒体会话的软件都可通过系统扬声器回录生成字幕。
- 外部视频播放现在优先于隐藏 SoundCloud 收藏曲目；进入视频模式会停用旧歌曲时间轴，避免歌词覆盖电影对白。
- 观看应用位于前台时，仅媒体字幕卡片单独悬浮显示，其他桌面组件继续隐藏；停止播放后恢复原来的桌面专属行为。
- 优化电影对白切句与 GPU 解码参数，降低字幕等待时间；长时间无声时停止回录但保留已预热模型。
- 字幕语言菜单扩展到 19 种明确语言加自动识别，所有输出继续使用“简体中文 + 原声文字”双行布局。
- 修复隐藏 SoundCloud WebView 缺少歌手元数据时被误判为视频的问题。
- 已使用本地英文电影对白完成端到端验证，确认系统回录、自动语言检测、简体中文翻译、原文显示和媒体卡片悬浮全部生效。
- 修复播放时歌词与声音不对应的问题：收藏曲目改为使用带时间戳的同步歌词，并以播放器毫秒进度定位当前行。
- 当前曲目已生成 36 条双语时间轴缓存；播放、暂停和进度变化时同步切换“简体中文 + 原文”。
- 实时 Whisper 改为无时间轴歌词时的备用方案；歌曲模式关闭重复 VAD 过滤，并在手动语言选错时自动回退检测。
- 字幕模型改为组件启动后后台预热，静音时只停止回录、不卸载模型，减少再次播放的等待时间。
- 修复音乐组件字幕被媒体元数据或短暂无声状态覆盖的问题；最后一条有效歌词会持续显示到歌曲切换。
- 音乐组件现在固定显示两行：第一行为简体中文翻译，第二行为歌曲原文，不再把所有原文误标为英语。
- 新增歌词原语言选择菜单，支持自动识别、英语、日语、韩语、西班牙语、法语、德语、俄语、泰语和中文，选择会在下次开机保留。
- 字幕工作进程新增语言提示命令，手动语言模式直接传给本地 Whisper，中文翻译仍统一输出为简体中文。
- 修正收藏曲目名称：中文显示“依然与你同在”，原名显示 `Still With You · Jung Kook`。
- 新增隐藏的 SoundCloud 官方嵌入播放器，开机自动播放，播放器音量固定为 24%，不修改系统总音量且不弹出浏览器。
- 修复媒体曲名被字幕等待或翻译失败状态覆盖的问题；字幕尚未生成时保持显示当前曲名和歌手。
- 字幕第二行改为原语言识别文本，第一行保留简体中文翻译；移除无用的英文二次识别以降低 CPU 占用。
- 修复语音与字幕运行环境指向已卸载 Python 的问题，改用项目内便携 Python 3.11.9。
- 将背景曲目、微信 Dock 状态点和 Dock 媒体进度并入统一开机任务，并停用重复的 Rainmeter 音乐卡片。

## 2026-08-01

- 新增独立可拖动的粉色“今日锁屏”方形组件，尺寸与时钟、日历、电池组件一致，并放在三者左侧。
- 组件显示每日累计时长、锁屏次数，以及每次锁屏的开始时间、结束时间和精确时长。
- 使用已保存的 `byte-bunny` 动物素材，为粉色卡片增加持续待机动作和悬停互动。
- 锁屏追踪状态升级到 v3，逐次保存会话；跨午夜锁屏会在零点拆分，旧日归档、新日自动从零开始。
- 粉色组件新增每日开机、关机时间线；同一天多次开关机会分别编号并逐条显示。
- 开关机记录来自 Windows Kernel-General 系统事件，并与本地 `power-history.json` 去重合并，跨日刷新时保留历史。
- 保留旧版累计状态的独立备份，未删除任何已有记录或个人文件。

## 2026-07-29

- 在“音乐可视化”卡片中央新增中文与 English 两行电影字幕，沿用原卡片，不打开网页或额外窗口。
- 使用 PyAudioWPatch 的 WASAPI loopback 直接读取默认系统输出，Whisper large-v3-turbo 在本机识别对白并生成英文。
- 对英文和其他非中文对白增加短文本中文翻译回退；网络不可用时保留英文原文，音频永不上传。
- 检测到系统声音后自动按需加载，长时间无声后自动释放；与现有麦克风听写互斥，避免重复加载模型。

## 2026-07-18

- 新增独立可拖动的“应用使用时间”组件，显示当天总时长、当前应用和前四名应用。
- 使用 5 秒低频前台窗口采样，只记录应用名称与累计秒数，每分钟最多写盘一次。
- 增加每日自动归零、位置保存和 `restore_app_usage_widget.ps1` 单项恢复脚本。
- 将组件显示策略改为桌面专属模式：任何普通应用进入前台时隐藏，回到桌面后恢复，避免组件覆盖 Codex、微信、浏览器等窗口。
- 将原来的简单媒体开关改成“音乐可视化”面板。
- 接入 Windows GlobalSystemMediaTransportControlsSession，显示真实媒体应用、歌曲、歌手、播放状态和进度。
- 接入 Windows Core Audio 峰值计量，增加 18 段实时输出频谱。
- 保留上一曲、播放/暂停、下一曲和已有媒体按键功能。
- 七个组件改为独立工具窗口，不再合并成任务视图中的普通后台卡片。
- 使用 VirtualDesktopAccessor 将组件固定到所有虚拟桌面。
- 修复切换任务视图、虚拟桌面和“显示桌面”后组件消失的问题。
- 保留组件独立拖动、布局保存、全屏影院避让、语音、任务、天气、照片、日历、时钟和电池功能。
- 增加修改前的完整备份与验证截图。
