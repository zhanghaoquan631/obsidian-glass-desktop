# Optional Local Artwork

The public package deliberately does not include a character, anime, wallpaper, screenshot, or other user-owned background image.

To use a background, keep an image on your own PC and pass its path explicitly to `apply-start-menu-theme.ps1 -Apply -BackgroundImage`. The image stays on that PC; this folder is only documentation and contains no art asset.
